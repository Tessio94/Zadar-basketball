export default function StatisticsTableTitle({ title }: { title: string }) {
    return (
        <h3 className="font-heading text-xl font-semibold text-slate-100">
            {title}
        </h3>
    );
}
